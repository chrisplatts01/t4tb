{% extends "layouts/layout.njk" %}

{% import "macros/section.njk" as section %}
{% import "macros/form.njk" as form %}

{% set page = {
    previous: "register-3.html",
    next: "register-5.html"
  }
%}

{% block content %}
  <section class="panel panel--progress">

    {{ section.header(
        title = "Register for the study",
        info = "<a href=\"#\">What happens to my data?</a>" | safe,
        duration = 15,
        step = 4,
        steps = 5
      )
    }}

    <article>
      <h1>Your date of birth</h1>
      <form id="register" method="get" action="{{ page.next }}">
        {{ form.text("dob", "Please enter your date of birth", "date", changeMonth = true, changeYear = true) }}
        {{ form.actions( page.previous, page.next ) }}
      </form>
    </article>

    {{ section.footer( info = "<a href=\"/\">Cancel and return to home page</a>" | safe ) }}

  </section>
{% endblock %}
